export global View {
    in-out property <string> backup-password;
    in-out property <string> backup-confirmation;
    in-out property <bool> crypto-busy;
    in-out property <int> page: 0;
    in-out property <string> title: "Inheritance";
    in-out property <bool> has-plan: false;
    in-out property <[string]> plan-names;
    in-out property <[string]> plan-summaries;
    in-out property <string> error;
    in-out property <string> notice;
    in-out property <string> wallet-name;
    in-out property <string> wallet-summary;
    in-out property <string> fingerprints;
    in-out property <string> address;
    in-out property <string> heir;
    in-out property <string> status;
    in-out property <string> readiness;
    in-out property <[string]> fields: ["", "", "", "", "", ""];
    in-out property <[bool]> checks: [false, false, false, false];
    in-out property <int> review-days: 90;
    in-out property <int> field-index: 0;
    in-out property <string> field-help;
    in-out property <string> edit-value;
    in-out property <int> guide-step: 0;
    in-out property <string> guide-title;
    in-out property <[string]> guide-headings;
    in-out property <[string]> guide-details;
    in-out property <[string]> signer-names;
    in-out property <[string]> signer-summaries;
    in-out property <[string]> signer-fields;
    in-out property <int> signer-index;
    in-out property <string> signer-fingerprint;
    in-out property <string> completeness;
    in-out property <bool> complete;
    in-out property <string> export-status;
    in-out property <bool> export-current;
    in-out property <string> revision;
    in-out property <string> rehearsal-status;
    in-out property <int> setup-step;
    in-out property <bool> field-multiline;
    in-out property <bool> duplicate;
    in-out property <string> duplicate-name;
    in-out property <int> rehearsal-step;
    in-out property <bool> rehearsal-confirmed;
    in-out property <string> rehearsal-title;
    in-out property <string> rehearsal-body;
    in-out property <[bool]> handover-checks;
    in-out property <string> handover-status;
    in-out property <[string]> rehearsal-key-headings;
    in-out property <[string]> rehearsal-key-details;
}
export global Actions {
    callback unlock-backup();
    callback crypto-finished();
    callback select-plan(int);
    callback go(int);
    callback back();
    callback import-wallet(bool);
    callback accept-wallet();
    callback edit(int);
    callback save-field();
    callback interval();
    callback save-plan();
    callback check(int);
    callback review();
    callback handover-check(int);
    callback handover-save();
    callback next-step();
    callback export-kit();
    callback import-kit();
    callback restore();
    callback delete();
    callback setup-next();
    callback select-signer(int);
    callback edit-signer(int);
    callback open-existing();
    callback rehearsal-confirm();
    callback rehearsal-next();
}
